# Звіт по лабораторній роботі №5
## Студент: Придорожко Денис Ігорович  
## Дисципліна: Технологія створення програмних продуктів

### 1. Мета роботи
- Коротко: ознайомлення з PostgreSQL та pgAdmin, створення й адміністрування БД, робота з CRUD, JOIN, агрегатами, процедурами та тригерами.

### 2. Середовище
- ОС: ______________________
- PostgreSQL версія: __________
- pgAdmin версія: ____________

### 3. Хід виконання (пункти інструкції)
Нумеруй та додай скріншоти після кожного ключового кроку.
1. Створення БД `financial_database_prydorozhko` (скрін/консоль).
2. Скидання/створення таблиць та початкове наповнення (виконання блоку 1, скрін).
3. Простi вибірки транзакцій за рахунком (3 запити, скрін результатів).
4. Сортування за датою та сумою (2 запити).
5. JOIN-и:
   - INNER JOIN (сума транзакцій по рахунку),
   - LEFT JOIN (рахунки без транзакцій),
   - CROSS JOIN (12 комбінацій),
   - FULL OUTER JOIN (accounts ↔ transactions).
6. Агрегатні запити (SUM/AVG/COUNT) з GROUP BY.
7. UPDATE балансу за типом + перевірка; UPDATE з JOIN для активних користувачів + перевірка.
8. DELETE старих транзакцій (>60 днів) + перевірка; DELETE для рахунків з негативним балансом + перевірка.
9. Рахунок з максимальною сумою транзакцій (без використання balance); користувачі з сумою транзакцій > 200.
10. Таблиця `categories` (створення, 10 записів, встановлення зв’язків, вибірка з категоріями).
11. Корельований підзапит для категорій із сумою > 100.
12. Stored procedure `calculate_balance_proc` (виклик, вивід); тригер `balance_trigger` (перевірка вставкою + зміна балансу).

### 4. Приклади SQL (для вставки в текст звіту)
- Додай 2–3 найпоказовіші запити з кожного блоку (JOIN, агрегати, процедура/тригер).

### 5. Результати спостережень
- Як змінився баланс після тригера?
- Який середній баланс за типами рахунків?
- Яка категорія/рахунок має найбільшу суму транзакцій?

### 6. Висновки
- Що дізнався про типи даних (VARCHAR, DECIMAL, DATE, BOOLEAN) та їх вплив на запити.
- Які інструменти PostgreSQL/pgAdmin були використані і чим були зручні.
- Потенційні покращення/подальші кроки.

### 7. Додатки
- Перелік виконаних файлів: `lab05.sql`, скріншоти (списком).
